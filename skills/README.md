# Skills

A skill is a single markdown file describing something the agent knows how
to do: `skills/<skill-name>.md`. Each file should explain, in plain
language, when to use the skill and the steps to follow.

Nothing in this directory is loaded by the harness yet — this is just the
directory and the naming convention, to be wired up in a later phase.
