# Ollama connection and default model settings for the harness.
base_url: http://localhost:11434
model: qwen3:14b
temperature: 0.1
num_ctx: 32768

# Ceiling on agent loop steps (harness/loop.py).
max_steps: 10
